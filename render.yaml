services:
  - type: web
    name: devmate-ai-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python ai.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: GITHUB_TOKEN
        sync: false
    healthCheckPath: /
